#include <stdio.h>
#define MAX 10 // Taille maximale du tableau

// Fonction qui calcule f(x) = x^2 + 4x + 1 pour un tableau donné
void calculF(int x[], int taille, int fx[]) {
    for (int i = 0; i < taille; i++) {
        fx[i] = x[i] * x[i] + 4 * x[i] + 1; // Calcul de f(x)
    }
}











.  b)

  Solution :
